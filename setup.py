from setuptools import find_packages, setup

setup(
    name='sdcdup',
    packages=find_packages(),
    version='0.1.0',
    description='Finding and tagging duplicate satellite images based on overlapping sub tiles.',
    author='Willie Maddox',
    license='MIT',
)
